# Diamond Resort Backend

This is the Node.js backend for the Diamond Resort application. It handles booking logic, admin authentication, and MongoDB integration.

## Setup
1. Run `npm install`
2. Configure `.env` with MongoDB URI
3. Run `npm start`
